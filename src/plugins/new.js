// import something here

// leave the export, even if you don't use it
export default ({ app, router, Vue }) => {
    Vue.prototype.$newPluginName = 'Esse é o nome do novo plugin';
  // something to do
}
